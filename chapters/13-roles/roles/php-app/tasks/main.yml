---

- name: Install php
  package:
    name: php83
    state: present

- name: Install nginx
  package:
    name: nginx
    state: present

- name: 'Deploy nginx configuration for FreeBSD'
  template:
    src: nginx-freebsd.conf.j2
    dest: /usr/local/etc/nginx/nginx.conf
    backup: yes
  notify: restart nginx

- name: Copy application
  template:
    src: "{{ item }}"
    dest: "/usr/local/www/nginx/index.php"
  loop:
    - roles-example.php
  notify:
    - restart nginx
